apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-nba-crawler.fullname" . }}-test-connection"
  labels:
    {{- include "go-nba-crawler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-nba-crawler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
