apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-nba-crawler.fullname" . }}
  labels:
    {{- include "go-nba-crawler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-nba-crawler.selectorLabels" . | nindent 4 }}
